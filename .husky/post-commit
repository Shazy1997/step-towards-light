#!/bin/sh

# Check if this is a documentation update commit
if git log -1 --pretty=%B | grep -q "docs: Update documentation"; then
  exit 0
fi

# Update documentation
npm run docs:update

# Stage documentation changes
git add docs/ PRODUCTION_SETUP.md

# Commit documentation changes
git commit --message "docs: Update documentation [skip ci]" || true
